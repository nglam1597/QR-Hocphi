# deps
node_modules
.pnpm-store

# next / turbo
.next
.turbo
out
dist

# env
.env
.env*.local

# misc
.DS_Store
*.log

# Excel gốc (PII) — KHÔNG commit; dữ liệu deploy dùng JSON đã sinh bên dưới
/input/*.xlsx
/input/*.xls

# NOTE: data/records.json, public/qr, public/index (đã sinh từ Excel) ĐƯỢC commit
# để Vercel build không cần Excel. Chúng chứa PII -> repo PHẢI để Private.
